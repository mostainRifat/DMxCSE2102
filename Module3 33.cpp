#include<bits/stdc++.h>
using namespace std;

int main()
{
    int k;
    cout << "Enter the dimensions of matrix A : ";
    cin >> k;

    int matA[k][k];
    
    cout << "Enter the elements of matrix A: "<<endl;
    for (int i = 0; i < m; i++)
        for(int j = 0; j < k; j++)
            cin >> matA[i][j];
return 0;
}